// UIRootType enum: 用于以枚举标识可控制的 UI 根
public enum UIRootType
{
    None = 0,
    Left,
    Right,
    Top,
    Bottom,
    MonsterBook,
    SideMenu,
    Inventory,
    Dialogue,
    MainMenu,
    // 在此添加更多 UI 类型
}
